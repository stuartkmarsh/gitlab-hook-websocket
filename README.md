gitlab-hook-websocket
=====================

This uses SockJS tornado to create a websocket server which outputs messages from a global queue.

The queue gets loaded from a Gitlab webhook post.

Work in progress.
